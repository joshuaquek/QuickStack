{
  "name": "QuickStack",
  "version": "1.0.0",
  "description": "Production-grade NodeJS web application template using - React (Front-end), NextJS (Routing), Express (Back-end), LokiJs (Shared Cache), PassportJs (Auth) and Mongo (Database)",
  "repository": "https://github.com/joshuaquek/QuickStack.git",
  "main": "server.js",
  "engines": {
    "node": ">=8.9.0",
    "npm": ">=5.3.0"
  },
  "private": true,
  "scripts": {
    "--------": " ---------------- MAIN COMMANDS ---------------- ",
    "build-server": "rimraf ./server/.next/;flow-remove-types --quiet --pretty ./server/ --out-dir ./server/.next/ --ignore ./server/.next/;",
    "dev": "nodemon -q -L ./server/.next/server.js; ##------- COMMENT: `npm run build-server` is run inside nodemon.json for dev -------##;",
    "start": "next build client;npm run build-server;NODE_ENV=production node ./server/.next/server.js;",
    "---------": " ---------------- DEVELOPER TOOLS ---------------- ",
    "flow": "flow",
    "package-versions": "npm list --depth=0"
  },
  "author": "Joshua Quek",
  "license": "MIT",
  "dependencies": {
    "bcrypt-nodejs": "0.0.3",
    "bluebird": "^3.5.1",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "lodash": "^4.17.10",
    "mongoose": "^5.1.3",
    "next": "^5.1.0",
    "react": "^16.3.1",
    "react-dom": "^16.3.1"
  },
  "devDependencies": {
    "babel-eslint": "8.2.1",
    "babel-plugin-transform-flow-strip-types": "6.22.0",
    "flow-bin": "0.64.0",
    "flow-remove-types": "1.2.3",
    "nodemon": "1.17.5",
    "rimraf": "2.6.2"
  }
}
